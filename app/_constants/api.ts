export const API_URL = process.env.NEXT_PUBLIC_API_URL || "";

export const API_USER_KEY = "/user" as const;

export const API_CSRF_COOKIE = "/csrf-cookie" as const;

export const API_SESSION_KEY = "papers_session" as const;
